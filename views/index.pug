extends layout

block content
  h1= title
  p Welcome to #{title}

  if user
    p Logged in
    p Username: #{user.username}
    p Status: #{user.status}
    if user.status=='Member'
      a(href='/join')
        button Join
    else if user.status=='Avenger'
      a(href='/leave')
        button Leave